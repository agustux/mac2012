#!/bin/bash

curl -o /tmp/rss-output-source.txt {{ rss }}
diff /tmp/rss-output-source.txt /tmp/rss-output-target.txt
EXIT_STATUS=$?
cp /tmp/rss-output-source.txt /tmp/rss-output-target.txt
if [ "$EXIT_STATUS" -ne 0 ]; then exit 1; else exit 0; fi
